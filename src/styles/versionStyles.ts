import { makeStyles } from "@mui/styles";

const versionStyles = makeStyles({
  versionDisplay: {
    position: "fixed",
    bottom: 10,
    right: 10,
    backgroundColor: "#f1f1f1",
    padding: 5,
    fontSize: 12,
    borderRadius: 4,
    zIndex: 2,
  },
});
export default versionStyles;
