import { makeStyles } from "@mui/styles";

const predictionStyles = makeStyles({
  predictionsTable: { marginTop: "2vh" },
  tableText: { fontWeight: "bold !important" },});
export default predictionStyles;
